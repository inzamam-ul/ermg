import React from "react";
import { Tag } from "@chakra-ui/react";

export const NotificationTag = (props) => (
  <Tag rounded="full" variantColor="orange" variant="solid" ml={2} minW="1rem" minH="1rem" px="0" {...props} />
);
